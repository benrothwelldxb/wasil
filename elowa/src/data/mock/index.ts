/**
 * Static reference content that ships with the app (not user data).
 * In Phase 1 this is limited to the Learn articles; user tracking data lives in
 * the persistence repositories, and demo data in `@/data/demo`.
 */
export { MOCK_ARTICLES, findArticle } from './articles';
