/**
 * FPL feature — public API.
 *
 * The data layer: typed models, the API service (endpoints + normalizers),
 * and TanStack Query hooks. No squad-selection or other product features yet.
 */
export * from "./types";
export * from "./api";
export * from "./components";
