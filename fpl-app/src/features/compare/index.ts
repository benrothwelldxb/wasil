/**
 * Compare — head-to-head "my team vs a rival" by Manager ID. Projects both
 * squads the same way and renders a shareable scorecard.
 */
export { projectSquad } from "./project";
export * from "./components";
