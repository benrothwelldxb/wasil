/**
 * Static asset re-exports (SVGs, images, fonts). Import assets here and
 * re-export them so consumers reference a stable module path.
 *
 * Empty for now — no bundled assets in the foundations scaffold.
 */
export {};
