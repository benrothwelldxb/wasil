# Cloudflare Pages SPA fallback: serve index.html for client-side routes.
# The API is proxied in dev; in production, point /api/* at your own backend
# or a Cloudflare Function/Worker (see README).
/*    /index.html   200
