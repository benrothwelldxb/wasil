/**
 * Brand configuration.
 *
 * "Bloom" is a TEMPORARY working name. Keeping it here (and reading from this
 * module everywhere) means the final name can be swapped in one place.
 */
export const BRAND = {
  name: 'Bloom',
  tagline: 'Understand your change',
  /** One-line product promise. */
  promise: 'Track. Understand. Act.',
} as const;
